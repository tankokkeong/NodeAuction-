<nav class="navbar navbar-expand-lg bg-nav">
    <a class="navbar-brand" href="home">Auction</a>
    <button class="navbar-toggler text-light" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"><i class="fas fa-bars"></i></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        
        </ul>
        <form class="form-inline my-2 my-lg-0">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="home">Home</a>
            </li>

            <li class="nav-item">
                <%  if(authenticated){ %>
                    <% if(accountType == "bidder"){ %>
                        <a class="nav-link" href="bidder-profile">My profile</a>
                    <% }else{ %>
                        <a class="nav-link" href="auctioneer-profile">My profile</a>
                    <% } %>
                <%  }else{ %>
                    <a class="nav-link" href="login">Login</a>
                <%  } %>
            </li>

            <%  if(authenticated){ %>
                <% if(accountType == "bidder"){ %>
                    <li class="nav-item">
                        <a class="nav-link" href="shopping-cart">My Cart</a>
                    </li>
                <% } %> 
            <%  }%>

            <li class="nav-item">
                <%  if(authenticated){ %>
                    <a class="nav-link" onclick="logout()" style="cursor: pointer;">Logout</a>
                <%  } %>
            </li>
        </ul>
        
        </form>
    </div>
</nav>